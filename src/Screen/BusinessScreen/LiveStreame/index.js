import React from 'react';
import {View, Text, Button} from 'react-native';

const LiveStreame = ({}) => {
  return (
    <View
      style={{flex: 1, alignItems: 'center', justifyContent: 'space-around'}}>
      <Button
        title="Start a live"
      
      />

      <Button
        title="Watch a live32342"
     
      />
    </View>
  );
};

export default LiveStreame;
